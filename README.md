## nested-npm-managment

Demo project showing how to use npm in nested folder

## How to run

```
$ cd project
$ npm run install
```

Now the `install.js` can only install the modules in the direct child folder.
